spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.216.128:3306/gmall_sms
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    bootstrap:
      enabled: true
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: properties
        namespace: dev-namespace
        group: DEFAULT_GROUP
        prefix: gmall-coupon
  application:
    name: gmall-coupon

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 7000
